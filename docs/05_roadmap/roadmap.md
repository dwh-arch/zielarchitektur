# Roadmap
## v0.1 (jetzt)
- Repo Skeleton + Local Runtime + dbt parse CI + Diagramme + ADRs

## v0.2
- n8n Beispiel-Flow (API/CSV -> Postgres ODS)
- Kafka Topic + Contract Beispiel
- Streamlit: einfache KPI Ansicht
- docker compose: optional schema registry / kafka UI

## v0.3
- Spark Job Template + Mini Streaming Consumer
- Data Vault Beispielmodel
- Power BI Semantik-Doku + Export-Konvention
